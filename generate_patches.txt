python -c "import os, glob; from PIL import Image; src='/home/swapna/AL_SSL_Den_Feb2016/data/KLSG_train'; out='./datasets/KLSG_train_patched'; os.makedirs(out, exist_ok=True); files=glob.glob(os.path.join(src, '*.png')); print(f'Found {len(files)} images in source'); [ [Image.open(f).crop((x, y, x+160, y+160)).save(os.path.join(out, f'p_{i}_{x}_{y}.png')) for x in range(0, Image.open(f).width, 160) for y in range(0, Image.open(f).height, 160) if x+160<=Image.open(f).width and y+160<=Image.open(f).height ] for i, f in enumerate(files) ]; print(f'Created {len(glob.glob(os.path.join(out, \"*.png\")))} patches in {out}')"
